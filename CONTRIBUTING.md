# Contributing

- SPEC is frozen (`docs/SPEC.md` v1.0). Propose changes as `docs/SPEC-v1.x.md`, never edit v1.0.
- Every run: fixed seed 7, next-open fills, gross AND net, cost ×1.5/×2 sensitivity, run JSON in `results/`.
- Preserve negatives: prefix rejected variants `REJECTED-`, never delete.
- No keys in repo (`.env` ignored). Sample CSVs only, ≤5 rows of real tape if licensed.
- Disclosures beside every figure (hypothetical, CFTC 4.41).
